import { ChevronLeft,ChevronRight } from "lucide-react";import {useState} from "react";
export function Pagination(){const [page,setPage]=useState(2);return <nav className="pagination" aria-label="Pagination"><button disabled={page===1} onClick={()=>setPage(p=>Math.max(1,p-1))} aria-label="Previous page"><ChevronLeft size={17}/></button>{[1,2,3,4].map(n=><button key={n} className={page===n?"is-active":""} aria-current={page===n?"page":undefined} onClick={()=>setPage(n)}>{n}</button>)}<button disabled={page===4} onClick={()=>setPage(p=>Math.min(4,p+1))} aria-label="Next page"><ChevronRight size={17}/></button></nav>}
