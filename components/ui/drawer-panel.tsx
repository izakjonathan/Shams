import {X} from "lucide-react";import {useState} from "react";import {ActionButton} from "./action-button";
export function DrawerPanel(){const[open,setOpen]=useState(false);return <><ActionButton variant="secondary" onClick={()=>setOpen(true)}>Open drawer</ActionButton>{open&&<div className="drawer-layer"><button className="drawer-scrim" onClick={()=>setOpen(false)} aria-label="Close drawer"/><aside className="drawer-panel" role="dialog" aria-modal="true" aria-labelledby="drawer-title"><button className="modal-close" onClick={()=>setOpen(false)} aria-label="Close drawer"><X size={18}/></button><span>Quick edit</span><h3 id="drawer-title">Component settings</h3><p>Use drawers for focused editing without leaving the current page.</p><label>Name<input defaultValue="Dashboard card"/></label><ActionButton onClick={()=>setOpen(false)}>Save changes</ActionButton></aside></div>}</>}
