import {X} from "lucide-react";import {useState} from "react";import {ActionButton} from "./action-button";
export function ModalDialog(){const[open,setOpen]=useState(false);return <><ActionButton onClick={()=>setOpen(true)}>Open modal</ActionButton>{open&&<div className="modal-layer" role="presentation" onMouseDown={()=>setOpen(false)}><section className="modal-card" role="dialog" aria-modal="true" aria-labelledby="modal-title" onMouseDown={e=>e.stopPropagation()}><button className="modal-close" onClick={()=>setOpen(false)} aria-label="Close modal"><X size={18}/></button><span>Confirmation</span><h3 id="modal-title">Publish this component?</h3><p>This pattern includes an overlay, labelled dialog and explicit close actions.</p><div><ActionButton variant="ghost" onClick={()=>setOpen(false)}>Cancel</ActionButton><ActionButton onClick={()=>setOpen(false)}>Publish</ActionButton></div></section></div>}</>}
